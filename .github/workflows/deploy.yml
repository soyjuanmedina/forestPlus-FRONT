name: Build & Deploy Angular App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build -- --configuration=production --base-href /forestplus/

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Deploy via lftp
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -c "
            set ftp:ssl-allow true
            set ftp:ssl-force true
            set ftp:ssl-protect-data true
            set ftp:passive-mode true
            set ssl:verify-certificate no
            open -u $FTP_USERNAME,$FTP_PASSWORD $FTP_SERVER

            # Crear carpeta forestplus si no existe
            mkdir -p /appservers/apache-tomcat-10x/webapps/forestplus

            # Limpiar solo los archivos y carpetas que vamos a subir
            cls /appservers/apache-tomcat-10x/webapps/forestplus/assets || true
            cls /appservers/apache-tomcat-10x/webapps/forestplus/chunks || true
            cls /appservers/apache-tomcat-10x/webapps/forestplus/*.js || true
            cls /appservers/apache-tomcat-10x/webapps/forestplus/*.css || true
            cls /appservers/apache-tomcat-10x/webapps/forestplus/index.html || true

            # Subir build Angular a /forestplus
            mirror -R --verbose dist/forest-plus/browser /appservers/apache-tomcat-10x/webapps/forestplus
          "
